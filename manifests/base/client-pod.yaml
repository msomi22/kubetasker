apiVersion: v1
kind: Pod
metadata:
  name: kube-tasker-client
  namespace: kubetasker
  labels:
    app: kube-tasker-client
spec:
  restartPolicy: Never
  containers:
    - name: client
      image: busybox:1.36
      command: ["sh", "-c", "sleep 3600"]
