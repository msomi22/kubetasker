apiVersion: v1
kind: Service
metadata:
  name: kube-tasker-api
  namespace: kubetasker
spec:
  type: ClusterIP
  selector:
    app: kube-tasker-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
